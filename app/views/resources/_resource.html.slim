li
  - if resource.link?
    = link_to resource.title, resource.link, class: 'underline'
  - elsif resource.document? && resource.document.present?
    = link_to resource.title, url_for(resource.document), class: 'underline', target: '_blank'
  - else
    p.font-bold = resource.title