= render layout: 'admin/themes/layout_form' do

  = form_for(@theme, as: :theme, \
    url: admin_theme_path(@theme), html: {class: "form-horizontal"}) do |f|
    =  render partial: "form", locals: {f: f}